version: '3.8'

services:
  rezept-hopper:
    build: .
    container_name: rezept-hopper
    ports:
      - "8080:8080"
    volumes:
      - rezept_hopper_data:/app/data
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    restart: unless-stopped

volumes:
  rezept_hopper_data:
