services:
  rezepthopper:
    build: .
    container_name: rezepthopper
    ports:
      - "8080:8080"
    environment:
      - Gemini__ApiKey=${GEMINI_API_KEY}
    volumes:
      - rezepthopper-data:/app/data
    restart: unless-stopped

volumes:
  rezepthopper-data:
